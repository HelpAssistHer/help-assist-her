language: node_js

node_js:
  - 7.9.0

cache:
  - yarn: true
  - directories:
    - node_modules

services:
  - mongodb

before_script:
  - sleep 15
  - mongo hah-test --eval 'db.addUser("travis", "test");'

script:
  - yarn lint
  - yarn test
  - yarn wds &

